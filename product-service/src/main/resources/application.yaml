server:
  port: 8082

spring:
  application:
    name: product-service

  config:
    import: optional:nacos:product-service.yml

  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml